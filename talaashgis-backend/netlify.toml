[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Serve the existing TalaashGIS frontend (public/index.html) at the site root,
# instead of Next.js's default app/page.tsx.
[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200
